# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'firebaseset' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Firebase/Analytics', '7.6.0'
  pod 'Firebase/Auth', '7.6.0'
  pod 'Firebase/Firestore', '7.6.0'
  pod 'FirebaseFirestoreSwift', '7.6.0-beta'
  
  pod 'Firebase/Crashlytics', '7.6.0'
  pod 'Firebase/Storage', '7.6.0'
 
  pod 'FirebaseUI'
  pod 'LicensePlist'

# Recommended: Add the Firebase pod for Google Analytics


  # Pods for firebaseset

  target 'firebasesetTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'firebasesetUITests' do
    # Pods for testing
  end

end
